#!/bin/sh
set -eu
if [ "$(id -u)" != 0 ]
then
  exec sudo -n "$(readlink -f "$0")"
fi
apt update
apt upgrade --with-new-pkgs -y || apt-get upgrade -y
apt upgrade -y -t experimental --with-new-pkgs || apt-get upgrade -y -t experimental
apt autoremove --purge -y
apt clean
