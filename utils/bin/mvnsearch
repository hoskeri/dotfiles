#!/bin/bash
set -eu
query=$1
curl -s "http://search.maven.org/solrsearch/select?q=${query}&rows=32&wt=json"|jq -r '.response.docs'|jq -r '.[]|["https://repo1.maven.org/maven2", (.g|gsub("\\."; "/")), .a, .latestVersion, (.a + "-" + .latestVersion)]|join("/")'

