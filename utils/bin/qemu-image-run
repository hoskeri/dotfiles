#!/bin/sh -eu
exec qemu-system-x86_64 \
  -enable-kvm -cpu host \
  -nographic \
  -net nic,model=virtio,macaddr=$(random-mac-address) \
  -net bridge,br=br0 \
  -m 512 \
  -cdrom $HOME/.qemu-image-builder/work/debootstrap.iso
